# Frontend environment example
# Base URL for API requests; if omitted, frontend will call same-origin
# e.g., when frontend runs at http://localhost:5173, backend should be proxied or CORS-enabled
VITE_API_BASE_URL=

# Optional socket URL; if empty the app will fall back to VITE_API_BASE_URL
VITE_SOCKET_URL=
